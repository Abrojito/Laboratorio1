spring.application.name=dishly


spring.datasource.url=jdbc:postgresql://localhost:5432/dishly
spring.datasource.username=dishly
spring.datasource.password=dishly123
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

logging.level.org.springframework.security=DEBUG

spring.data.web.pageable.default-page-size=3

app.google.clientId=${GOOGLE_CLIENT_ID:}

spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=dishly.demo@gmail.com
spring.mail.password=zupijdyorwdttoeq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

app.mail.from=${SMTP_FROM:${spring.mail.username}}
app.frontendBaseUrl=${APP_FRONTEND_BASE_URL:http://localhost:5173}
